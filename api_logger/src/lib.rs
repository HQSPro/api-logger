


pub struct Logger {
    name: String,
}
impl Logger {
    

pub fn logger_create(logger: &mut Logger) {
    
}
pub fn logger_create_with_name(name: &str, logger: &mut Logger) {
    
}


#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
